{
  "name": "eclipse-scout-homepage",
  "version": "0.0.1",
  "description": "Re-Design 2020",
  "keywords": [
    "eclipse",
    "scout"
  ],
  "license": "",
  "author": "André Wegmüller, BSI",
  "scripts": {
    "build": "echo \"Error: implement build\" && exit 1",
    "dev:server": "node server.js",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-handlebars": "^5.2.0",
    "express-less": "^0.1.0"
  }
}
